import React, { Component } from "react";
import { Grid, Cell } from "react-mdl";

class Skill extends Component {
  render() {
    return (
      <div className="resume-information">
        <Grid>
          <Cell col={12}>Not going to follow tut</Cell>
        </Grid>
      </div>
    );
  }
}

export default Skill;
